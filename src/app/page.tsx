"use client";

import { FC } from "react";
import VideoDisplayer from "@/components/VideoDisplayer";

const Home: FC<{}> = ({ }) => {
  return (
    <main>
      <VideoDisplayer />
    </main>
  );
}

export default Home;